---
description: 
globs: src/**/*.tsx
alwaysApply: false
---
- Use kebab-case for file names
- All component names should use PascalCase
- Use react-hook-form to create forms and form validation rules
- Use SCSS Modules as the styling standard
- Use type instead of interface
- Add data-testid to interactive controls (e.g., `input-username`)
